import React from "react";

function Experience() {
	return (
		<section
			style={{
				height: `${window.innerHeight}px`,
			}}
			className=""
      id = "work"
		></section>
	);
}

export default Experience;
